import { StyleSheet } from 'react-native';
import { pacificBlue, beauBlue } from '../../../constants/colors';

const styles = StyleSheet.create({
  container: {
    backgroundColor: beauBlue
  },
  header: {
    backgroundColor: pacificBlue
  },
  content: {
    margin: 10,
  }
})

export default styles;